#include<stdio.h>
#include<stdlib.h>
int main()
{
    int num, i;
    printf("enter the number:");
    scanf("%d", &num);
    for(i=1; i<=num;i++)

    switch(i<=num)
    {
       case 1 : printf("%d \n",i);
        break;
        default : exit(0);
    }
   
}